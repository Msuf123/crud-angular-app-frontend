import { TemplateDrivenDirective } from './template-driven.directive';

describe('TemplateDrivenDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateDrivenDirective();
    expect(directive).toBeTruthy();
  });
});
